import { Navbar } from "react-bootstrap";

function NavBar() {

    /*if (navbar ) */
  return (


    <nav>
      <a>Link</a>
      <a>Link</a>
      <a>Link</a>
    </nav>
  )
}

export default NavBar;

//con parametros lo hacemos dinamico

NavBar(true)
Navbar(false)
